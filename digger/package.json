{
  "name": "digger-ts",
  "version": "1.0.0",
  "private": true,
  "description": "Classic Digger arcade game",
  "dependencies": {
    "@assemblyscript/loader": "^0.19.5"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.5"
  },
  "scripts": {
    "build": "webpack",
    "test": "echo \"Error: no test specified\" && exit 1",
    "asbuild": "asc main.ts --baseDir src --binaryFile ../obj/main.wasm --exportRuntime --sourceMap --measure --runtime incremental -O3 --target release --converge"
  },
  "author": "xafero",
  "license": "AGPL-3.0-or-later"
}
